<div class="col-md-6">
  <div class="row top-header">Edit Room</div>
  <div class="content">
    <%= form_for @room do |f| %>
      <%= render partial: 'form', locals: {f: f} %>
    <% end %>
  </div>
</div>